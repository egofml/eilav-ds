# 상품 편집실 사용 방법

## 시작

1. 앱을 열면 2025년 금지어 1,470개가 자동 적용된다. 금지어 파일 업로드는 필요 없다.
2. Excel의 열 제목과 상품 행을 함께 복사해 웹앱에 붙여넣는다.
3. 두 배송비 인상액과 키워드 위치를 정하고 표를 불러온다.
4. 금지어 삭제 결과와 배송비를 확인한다. 추가 키워드는 상품별 또는 현재 검색 결과에 대해 일괄 검토 후 적용한다.
5. ‘상품명~반품배송비 13열 복사’를 누르고 원래 Excel 상품명 열의 첫 데이터 셀에 값으로 붙여넣는다. 가운데 가격·이미지·브랜드 등은 원본 값을 포함한다. 해당 13개 열이 입력 표에서 같은 순서로 연속되어 있어야 하며, 열 제목은 복사하지 않는다. 기존 개별 열 복사도 사용할 수 있다.

최대 20,000행, 30MB. 필수 열은 상품코드·상품명·키워드·배송비·반품배송비다. 다른 열, 빈 행, 원본 행 순서를 유지한다. 검색 필터는 출력 행을 줄이지 않는다. 원래 Excel을 정렬하거나 행을 삭제한 후 붙여넣으면 위치가 어긋날 수 있다.

기본 인상액은 두 열 각각 500원이다. 숫자 0원은 기본적으로 그대로 유지한다. ‘0원 배송비에도 인상액 적용’을 직접 체크한 경우에만 인상한다. 빈 셀·무료 같은 문자·잘못된 숫자는 변경하지 않고 표시한다. 재계산은 입력 원본에서 시작하므로 중복 인상하지 않는다.

전체 TSV는 값 데이터이며 수식·서식·병합 셀을 복원하지 않는다. 수식처럼 해석될 수 있는 문자열에는 작은따옴표를 붙여 내보낸다. 전체 TSV를 가져올 때 상품코드 등은 Excel에서 텍스트 열로 지정한다. 원래 파일의 서식을 유지하려면 바뀐 열만 값으로 붙여넣는다.

## 금지어와 상품명

- 금지어는 상품명과 키워드에서 대소문자를 구분하지 않고 부분일치로 삭제한다. 정규식 문법으로 해석하지 않는다.
- 짧은 단어는 정상 단어 일부도 지울 수 있으므로 삭제된 표현과 결과를 확인한다. 상품명이 비면 상품명 및 전체 표 내보내기를 막는다.
- 제공된 XLSM 형식은 `금지어삭제` 시트 B5 제목을 확인한 후 B6부터 읽는다. 1,470개 목록 추출을 검사했다. 매크로·수식·외부 링크를 실행하지 않는다.
- 사용자 요청에 따라 금지어 셀 1,470개만 앱과 공개 저장소에 내장한다. 원본 강의 파일·매크로·개인 경로는 포함하지 않는다. 선택 항목에서 목록을 교체하거나 내장 목록으로 복원할 수 있다. 교체한 목록은 현재 페이지에만 적용되며 다시 열면 내장 목록으로 시작한다.
- 금지어를 제거한 원래 상품명은 유지하고, 기존 키워드 중 이름에 없는 표현을 최대 1~3개 추가 후보로 제안한다. AI가 의미와 수식 관계를 보고 원본 상품명의 중간 띄어쓰기 사이를 선택한다. 중간 삽입을 우선하되 앞이나 뒤가 더 자연스러우면 AI가 이유와 함께 선택할 수 있다. 어느 위치에도 적절하지 않은 후보는 제외한다. 단어·규격·수량의 내용과 순서는 앱이 그대로 보존하고, AI는 원본 기준 삽입 위치 번호와 이유만 반환한다. AI 검토 전에는 규칙으로 만든 상품명 제안을 일괄 적용하지 않는다. 사용자 승인 전에는 추가하지 않는다. 보류해도 요청된 금지어 삭제는 적용한다.
- 금지어 제거와 AI 검토는 저작권·상표권 판정이 아니다. 이름에서 단어를 지워도 실제 제품·이미지·캐릭터·판매 허가의 문제는 남을 수 있다.

## Gemini와 여러 키

API 키 없이 기본 편집을 사용할 수 있다. Gemini는 일반 지식으로 의심 표현을 제안하는 선택 기능이다. 실시간 Google 검색과 KIPRIS 자동 조회는 연결하지 않았다.

본인의 키를 이름과 실제 Google 프로젝트 ID로 최대 20개 등록한다. 각자의 브라우저 localStorage에 저장하고 다음 접속에 복원한다. 이름·프로젝트·키·선택 상태·한도 중지 상태를 유지하며 삭제 버튼은 저장본도 제거한다. 암호화 저장이 아니므로 개인용 브라우저에서 사용한다. 같은 컴퓨터라도 브라우저나 주소(포트 포함)가 다르면 별개이며, 브라우저 데이터 삭제 또는 시크릿 모드에서는 유지되지 않을 수 있다. 서버나 공개 저장소에 키를 저장하지 않는다. 기존 메모리 전용 버전에 입력한 키는 업데이트 후 한 번 다시 등록해야 한다.

같은 프로젝트는 키가 달라도 한도를 공유한다. HTTP 429를 받으면 해당 프로젝트를 중지하며, 자동 전환이 선택된 경우 이미 등록한 다른 프로젝트를 한 번씩 시도한다. 인증·형식·네트워크 오류에는 자동 반복하지 않는다. 모두 중지되면 멈춘다. 한도를 확인한 뒤 중지를 직접 해제하며 최소 60초 또는 서버가 지정한 대기시간 전에는 해제할 수 없다. 일일 한도는 이 대기시간만으로 회복되지 않는다.

전체 상품 AI 검토 버튼을 누르면 검색·페이지와 관계없이 추가 후보가 있는 미검토 상품 전체를 선택한 묶음 크기로 Google에 전송한다. 기본 20개이며 서로 다른 프로젝트 최대 4개가 동시에 처리한다. 묶음 사이에는 1.5초 간격을 둔다. 중지하면 현재 묶음을 마친 뒤 멈추고, 완료된 상품은 이어하기에서 건너뛴다. 오류·한도 초과 시 완료 결과를 유지하며 자동 재시도를 무한 반복하지 않는다. 표·규칙 변경 시 진행 중 응답은 적용하지 않는다. 자동 저장 완료 후 새로고침하면 저장된 작업을 복원해 이어갈 수 있다. 전송 필드는 원본 행 번호, 정리된 상품명·추가 후보 키워드(최대 3개), 브랜드·카테고리(각 앞 200자)다. 상품코드·가격·상세정보·주문정보·이미지는 전송하지 않는다. AI는 추가 후보만 검토한다. 의심 또는 불확실 후보는 상품명 삽입에서 자동 제외하며, 나머지도 사용자 승인 후 반영한다. 키워드 원본 열에서도 삭제하려면 전체 금지어에 추가한다. 의심 표현이 없는 응답도 법적 안전 확인은 아니다.

무료 한도는 계정과 모델에 따라 다르다. 유료 프로젝트는 요금이 발생할 수 있으며 무료 이용 데이터는 Google 제품 개선에 사용될 수 있다. 기본 모델은 `gemini-3.1-flash-lite`이며 변경할 수 있다. 공식 REST 요청 및 모의 응답 검증은 했으나 실제 키를 사용한 응답 품질과 성공 호출은 아직 검증하지 않았다.

공식 참고:

- https://ai.google.dev/gemini-api/docs/rate-limits
- https://ai.google.dev/gemini-api/docs/pricing
- https://ai.google.dev/api/generate-content
- https://www.kipris.or.kr/

## 실행과 개발

`npm start`로 실행하고 http://127.0.0.1:5549 에 접속한다. 서버는 해당 PC에서만 접속된다. ZIP의 `dist/index.html`을 직접 열 수도 있지만 브라우저 제한이 있는 경우 로컬 서버를 사용한다. 인터넷 공유에는 배포된 웹앱 주소를 사용한다.

다른 PC에서는 같은 소스를 본인의 Codex 계정으로 열어 이어서 개발한다. 상품 표·검토 결과·수정 규칙은 같은 브라우저의 IndexedDB에 자동 저장하며 새로고침 시 복원한다. 다른 PC로 이동하려면 키가 빠진 작업 JSON 파일을 저장해서 불러온다.

개발 검사는 `npm ci` 후 `npm test`. TSV 왕복, 줄바꿈·따옴표·빈 행, 부분일치, 중복 가산 방지, 20,000행 처리, 금지어 승인 차단, Gemini 모의 응답과 프로젝트별 전환을 검증한다. 제공 XLSM 검사는 해당 로컬 파일 경로를 `DS_FORBIDDEN_PATH` 환경변수로 설정할 때 실행된다. 원본 파일은 테스트에 포함하지 않는다.

실제 Excel 자동 쓰기는 구현하지 않았으며 실제 Excel 붙여넣기 결과는 별도 확인이 필요하다. WebMCP는 지원 브라우저에서만 활성화되며 브라우저 통합 검증은 미완료다.

키별 누적 요청 횟수, JSON 응답 횟수, 응답의 usageMetadata 기반 입력·출력·총 토큰을 표시하고 브라우저에 저장한다. 실패한 요청도 요청 횟수에 포함되며 토큰 정보가 없는 응답은 토큰을 추정하지 않는다. 전체 Google 계정 사용량이나 남은 한도가 아니다. 한 탭에서 작업한다. 키 삭제 시 해당 사용 기록도 삭제된다.

개인 사용 흐름: 별도 동의·권리 확인·내보내기 체크박스 없이 AI 시작, 개별 수정 적용, 일괄 적용, 복사 버튼으로 바로 실행한다. AI가 제외한 후보와 금지어 차단, 빈 상품명 방지 등 데이터 검증은 유지한다.

각 키의 ‘연결 테스트’는 현재 모델 ID에 가상 상품 1개를 실제 검토 형식으로 보낸다. 해당 키만 테스트하며 다른 키로 자동 전환하지 않는다. 성공 시 모델·응답 시간·시각, 실패 시 HTTP 코드와 원인을 표시한다. 테스트 요청과 토큰은 누적 사용량에 포함한다. 90초 시간 제한을 적용하며 한도 초과 대기시간은 유지한다.

Gemini 응답은 명시적인 JSON 스키마를 요청하고 단독 JSON 코드블록을 허용한다. ID·후보·분류·삽입 위치 검증은 유지한다. 형식 오류 또는 길이 제한으로 잘린 응답에 한해 큰 묶음을 5개, 다시 1개로 분할 재요청한다. 단일 상품 실패는 표시하고 나머지를 진행한다. HTTP 한도·인증·네트워크 오류는 이 형식 복구 대상으로 취급하지 않는다. 분할 재요청도 요청·토큰 사용량에 포함된다.

## 모델 자동 선택

기본으로 켜져 있다. 검토 또는 키 연결 테스트를 시작하면 Google Models.list를 해당 키로 조회한다. generateContent 지원 및 출력 한도 8,192 이상인 안정 버전 중 Flash-Lite를 우선하고, 없으면 Flash를 선택한다. 같은 계열에서는 높은 버전 순이다. 미리보기·실험·Pro·이미지·음성 전용 모델은 자동 선택하지 않는다. 선택된 실제 모델 ID를 화면에 표시하고 이번 실행 동안 유지한다. 다음 시작/이어하기 시 목록을 다시 확인하므로 기존 ID가 목록에서 없어져도 새 안정 버전을 선택할 수 있다. 자동 선택을 해제하면 직접 지정할 수 있다. 목록 조회 실패나 알 수 없는 새 명명 규칙은 오류를 표시하며 임의 모델을 추측하지 않는다. 모델 목록은 무료 여부나 잔여 한도를 보장하지 않는다. 작업 도중 모델이 폐기되면 멈추며 시작/이어하기로 다시 선택한다.

연결 테스트는 가상 상품 1개를 실제 검토와 같은 JSON 스키마·설정으로 전송하고 결과를 검증한다. Google 오류는 HTTP 상태·선택 모델·키를 가린 상세 사유로 표시한다. 스키마에서 배열 개수 제약을 제거해 복잡도를 낮췄으며 상품 수·후보 수·삽입 위치 검증은 응답을 받은 뒤 유지한다.

## 등록 제외 검토와 처리 속도

상품명·키워드·옵션·상세정보·노트의 원문에서 14세 이상/부터, 14세 미만 사용금지·불가, 14세 이하 사용금지·불가 문구를 찾아 별도 분류한다. 인증 위반 판정이 아닌 사용자 판매 제외 기준이며, 이미지 글자나 외부 링크는 읽지 않는다. 해당 행은 원문을 유지하고 AI 및 일괄 수정에서 제외한다. 모아보기와 원본 행·상품코드·사유 목록을 제공한다. 기존 복사/전체 TSV는 행 정렬 보존 때문에 제외 행도 원문 그대로 포함한다. 제외 상품이 빠진 전체 표 파일은 새 시트에서 사용한다.

10/20/40개 묶음을 선택할 수 있고 기본은 20개다. 1.5초 대기 생략 옵션은 선택 사항이다. 큰 묶음은 요청 횟수는 줄지만 응답 시간·잘림·한도 초과로 더 느려질 수 있으며 실키 속도 비교는 아직 수행하지 않았다. 형식 오류는 5개, 다시 1개로 나눠 재시도하고 단일 상품도 실패하면 AI 개별 검토 실패로 표시한 뒤 나머지를 계속한다. 인증·한도 오류는 중지하며 일시적인 네트워크·500/502/503/504 오류는 2초·4초 간격으로 최대 두 번 재시도 후 중지한다. 실패 상품은 변경 제안을 적용하지 않고 시작/이어하기 때 재시도한다.

## 완료 흐름 검증과 복원

작업을 불러오거나 검토 결과·승인 상태가 바뀌면 원본 TSV, 규칙, 검토 결과만 IndexedDB에 저장한다. API 키는 작업 파일에 포함하지 않는다. 복원은 원본을 재분석하고 저장된 검토를 검증해 적용하므로 배송비를 다시 누적 가산하지 않는다. 저장 실패 상태가 보이면 작업 파일을 받아둔다. 입력만 바꾸고 재계산하지 않은 내용은 저장하지 않으며 마지막 계산된 작업까지만 복원한다. 기존 버전에서 아직 열려 있는 표는 소급 자동 저장되지 않는다. 브라우저 저장소 삭제·다른 주소·다른 브라우저는 별도 공간이며 한 탭에서 작업한다.

검토 중 표·규칙·개별/일괄 수정은 잠근다. 실패 상품까지 포함한 처리 진행률과 성공·실패 수를 구분한다. 보류·사용자 승인 행은 자동 재검토하지 않는다. 일괄 적용과 복원은 금지어 검색 구조를 재사용한다. 3,000개 가상 상품의 실패 분리·복원·내보내기를 테스트하고 실제 브라우저에서 모의 API로 처리, 적용, 새로고침 복원과 복사 버튼 성공을 확인했다. 실제 Gemini 응답 품질·계정 한도·Excel 앱의 최종 붙여넣기는 이 검증에 포함되지 않는다.

연령 탐지를 숫자 나이 전반으로 확대했다. 예: 3세 이상, 만 8세 이상, 14세 이상 사용, 36개월 미만, 15세 이하 사용금지. 세·살은 1~2자리 숫자, 개월은 1~3자리 숫자와 이상·이하·미만·초과·부터 표현을 탐지한다. 여러 문구가 있으면 발견한 열과 문구를 모두 기록한다. 표의 삭제 필요 검토 건수 안내, 모아보기, 삭제 검토 목록으로 확인한다. 인증 위반이나 실제 삭제 확정 판정은 아니며 기존처럼 원본 행을 유지한다. 저장된 작업을 복원할 때도 확대된 탐지 규칙을 적용한다.

## 여러 프로젝트 병렬 검토

Gemini 영역의 동시 작업 수는 1~4이며 기본 4다. 등록할 때 실제 Google 프로젝트 ID를 입력한다. 키가 여러 개여도 같은 프로젝트 ID면 작업자 하나로 묶는다. 준비된 서로 다른 프로젝트 수와 선택한 동시 작업 수 중 작은 수만큼 시작한다. 각 작업자는 한 요청을 마친 뒤 다음 상품 묶음을 가져가며, 응답 순서와 관계없이 원본 행에 적용한다. 프로젝트별 선택 모델과 진행 상태가 표시된다.

다른 프로젝트 전환 옵션이 켜져 있으면 한 작업자의 인증·한도·복구 불가능 오류 시 미완료 상품을 이번 실행의 남은 작업자가 이어받는다. 선택되지 않은 추가 프로젝트까지 자동으로 투입하지는 않는다. 작업자가 모두 멈추면 완료 결과를 저장하고 남은 개수를 알린다. 중지는 이미 전송한 묶음의 결과까지 받은 뒤 멈춘다. Google 한도는 키별이 아니라 프로젝트별이며 실제 속도는 모델과 계정 한도에 따라 달라진다. 3,000개 모의 응답으로 동시성, 응답 순서 변경, 개별 실패, 한도 초과 인계, 중복 적용 방지와 중지를 검증한다.

새 엑셀 작업은 표 불러오기 옆의 ‘새 작업 시작 · 초기화’를 누른다. 표·검토 결과·자동 저장 작업을 비우며 API 키·사용량·현재 금지어·수정 설정은 유지한다. AI 검토 중에는 중지 후 사용할 수 있다. 새 표를 불러오거나 새로고침하기 전에는 ‘초기화 되돌리기’로 복구할 수 있다. 이전 작업을 장기 보관하려면 먼저 작업 파일을 저장한다.
