# 상품 편집실 사용 방법

## 시작

1. 앱을 열면 2025년 금지어 1,470개가 자동 적용된다. 금지어 파일 업로드는 필요 없다.
2. Excel의 열 제목과 상품 행을 함께 복사해 웹앱에 붙여넣는다.
3. 두 배송비 인상액과 키워드 위치를 정하고 표를 불러온다.
4. 금지어 삭제 결과와 배송비를 확인한다. 추가 키워드는 상품별 또는 현재 검색 결과에 대해 일괄 검토 후 적용한다.
5. 상품명, 키워드, 배송비 2열을 각각 복사해 원래 Excel의 해당 열 첫 데이터 셀에 값으로 붙여넣는다.

최대 20,000행, 30MB. 필수 열은 상품코드·상품명·키워드·배송비·반품배송비다. 다른 열, 빈 행, 원본 행 순서를 유지한다. 검색 필터는 출력 행을 줄이지 않는다. 원래 Excel을 정렬하거나 행을 삭제한 후 붙여넣으면 위치가 어긋날 수 있다.

기본 인상액은 두 열 각각 500원이다. 숫자 0원은 기본적으로 그대로 유지한다. ‘0원 배송비에도 인상액 적용’을 직접 체크한 경우에만 인상한다. 빈 셀·무료 같은 문자·잘못된 숫자는 변경하지 않고 표시한다. 재계산은 입력 원본에서 시작하므로 중복 인상하지 않는다.

전체 TSV는 값 데이터이며 수식·서식·병합 셀을 복원하지 않는다. 수식처럼 해석될 수 있는 문자열에는 작은따옴표를 붙여 내보낸다. 전체 TSV를 가져올 때 상품코드 등은 Excel에서 텍스트 열로 지정한다. 원래 파일의 서식을 유지하려면 바뀐 열만 값으로 붙여넣는다.

## 금지어와 상품명

- 금지어는 상품명과 키워드에서 대소문자를 구분하지 않고 부분일치로 삭제한다. 정규식 문법으로 해석하지 않는다.
- 짧은 단어는 정상 단어 일부도 지울 수 있으므로 삭제된 표현과 결과를 확인한다. 상품명이 비면 상품명 및 전체 표 내보내기를 막는다.
- 제공된 XLSM 형식은 `금지어삭제` 시트 B5 제목을 확인한 후 B6부터 읽는다. 1,470개 목록 추출을 검사했다. 매크로·수식·외부 링크를 실행하지 않는다.
- 사용자 요청에 따라 금지어 셀 1,470개만 앱과 공개 저장소에 내장한다. 원본 강의 파일·매크로·개인 경로는 포함하지 않는다. 선택 항목에서 목록을 교체하거나 내장 목록으로 복원할 수 있다. 교체한 목록은 현재 페이지에만 적용되며 다시 열면 내장 목록으로 시작한다.
- 금지어를 제거한 원래 상품명은 유지하고, 기존 키워드 중 이름에 없는 표현을 최대 1~3개 추가 후보로 제안한다. 자동 위치는 끝의 규격·수량 앞이며 위치를 직접 선택할 수도 있다. 사용자 승인 전에는 추가하지 않는다. 보류해도 요청된 금지어 삭제는 적용한다.
- 금지어 제거와 AI 검토는 저작권·상표권 판정이 아니다. 이름에서 단어를 지워도 실제 제품·이미지·캐릭터·판매 허가의 문제는 남을 수 있다.

## Gemini와 여러 키

API 키 없이 기본 편집을 사용할 수 있다. Gemini는 일반 지식으로 의심 표현을 제안하는 선택 기능이다. 실시간 Google 검색과 KIPRIS 자동 조회는 연결하지 않았다.

본인의 키를 이름과 실제 Google 프로젝트 ID로 최대 20개 등록한다. 각자의 브라우저 localStorage에 저장하고 다음 접속에 복원한다. 이름·프로젝트·키·선택 상태·한도 중지 상태를 유지하며 삭제 버튼은 저장본도 제거한다. 암호화 저장이 아니므로 개인용 브라우저에서 사용한다. 같은 컴퓨터라도 브라우저나 주소(포트 포함)가 다르면 별개이며, 브라우저 데이터 삭제 또는 시크릿 모드에서는 유지되지 않을 수 있다. 서버나 공개 저장소에 키를 저장하지 않는다. 기존 메모리 전용 버전에 입력한 키는 업데이트 후 한 번 다시 등록해야 한다.

같은 프로젝트는 키가 달라도 한도를 공유한다. HTTP 429를 받으면 해당 프로젝트를 중지하며, 자동 전환이 선택된 경우 이미 등록한 다른 프로젝트를 한 번씩 시도한다. 인증·형식·네트워크 오류에는 자동 반복하지 않는다. 모두 중지되면 멈춘다. 한도를 확인한 뒤 중지를 직접 해제하며 최소 60초 또는 서버가 지정한 대기시간 전에는 해제할 수 없다. 일일 한도는 이 대기시간만으로 회복되지 않는다.

전체 상품 AI 검토 버튼을 누르면 검색·페이지와 관계없이 추가 후보가 있는 미검토 상품 전체를 20개씩 순서대로 Google에 전송한다. 묶음 사이에는 1.5초 간격을 둔다. 중지하면 현재 묶음을 마친 뒤 멈추고, 완료된 상품은 이어하기에서 건너뛴다. 오류·한도 초과 시 완료 결과를 유지하며 자동 재시도를 무한 반복하지 않는다. 표·규칙 변경 시 진행 중 응답은 적용하지 않는다. 새로고침 후 이어하기는 지원하지 않으므로 작업 중 페이지를 유지한다. 전송 필드는 원본 행 번호, 정리된 상품명(앞 1,000자)·추가 후보 키워드(최대 3개), 브랜드·카테고리(각 앞 200자)다. 상품코드·가격·상세정보·주문정보·이미지는 전송하지 않는다. AI는 추가 후보만 검토한다. 의심 또는 불확실 후보는 상품명 삽입에서 자동 제외하며, 나머지도 사용자 승인 후 반영한다. 키워드 원본 열에서도 삭제하려면 전체 금지어에 추가한다. 의심 표현이 없는 응답도 법적 안전 확인은 아니다.

무료 한도는 계정과 모델에 따라 다르다. 유료 프로젝트는 요금이 발생할 수 있으며 무료 이용 데이터는 Google 제품 개선에 사용될 수 있다. 기본 모델은 `gemini-3.1-flash-lite`이며 변경할 수 있다. 공식 REST 요청 및 모의 응답 검증은 했으나 실제 키를 사용한 응답 품질과 성공 호출은 아직 검증하지 않았다.

공식 참고:

- https://ai.google.dev/gemini-api/docs/rate-limits
- https://ai.google.dev/gemini-api/docs/pricing
- https://ai.google.dev/api/generate-content
- https://www.kipris.or.kr/

## 실행과 개발

`npm start`로 실행하고 http://127.0.0.1:5549 에 접속한다. 서버는 해당 PC에서만 접속된다. ZIP의 `dist/index.html`을 직접 열 수도 있지만 브라우저 제한이 있는 경우 로컬 서버를 사용한다. 인터넷 공유에는 배포된 웹앱 주소를 사용한다.

다른 PC에서는 같은 소스를 본인의 Codex 계정으로 열어 이어서 개발한다. 상품 표와 검토 기록도 브라우저 메모리에만 있으므로 필요한 결과는 다운로드한다.

개발 검사는 `npm ci` 후 `npm test`. TSV 왕복, 줄바꿈·따옴표·빈 행, 부분일치, 중복 가산 방지, 20,000행 처리, 금지어 승인 차단, Gemini 모의 응답과 프로젝트별 전환을 검증한다. 제공 XLSM 검사는 해당 로컬 파일 경로를 `DS_FORBIDDEN_PATH` 환경변수로 설정할 때 실행된다. 원본 파일은 테스트에 포함하지 않는다.

실제 Excel 자동 쓰기는 구현하지 않았으며 실제 Excel 붙여넣기 결과는 별도 확인이 필요하다. WebMCP는 지원 브라우저에서만 활성화되며 브라우저 통합 검증은 미완료다.

키별 누적 요청 횟수, JSON 응답 횟수, 응답의 usageMetadata 기반 입력·출력·총 토큰을 표시하고 브라우저에 저장한다. 실패한 요청도 요청 횟수에 포함되며 토큰 정보가 없는 응답은 토큰을 추정하지 않는다. 전체 Google 계정 사용량이나 남은 한도가 아니다. 한 탭에서 작업한다. 키 삭제 시 해당 사용 기록도 삭제된다.

개인 사용 흐름: 별도 동의·권리 확인·내보내기 체크박스 없이 AI 시작, 개별 수정 적용, 일괄 적용, 복사 버튼으로 바로 실행한다. AI가 제외한 후보와 금지어 차단, 빈 상품명 방지 등 데이터 검증은 유지한다.
