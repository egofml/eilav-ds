# 공통 사용자 작업 원칙

- 하겠습니다라고 말한 뒤 실행하지 않고 턴을 끝내지 않는다. 승인된 작업은 실행, 검증, 결과 전달까지 진행한다.
- 허락이나 필수 입력이 필요하면 구체적으로 질문한다. 진행 보고를 완료 답변으로 대신하지 않는다.
- 실제 장애 또는 사용자 중단 지시가 있을 때만 멈추고 상태를 정확히 알린다. 상위 안전 지침을 준수한다.

# 프로젝트 원칙

- 다른 PC와 다른 ChatGPT/Codex 계정에서도 재현 가능한 위탁판매 자동화를 만든다.
- README.md에서 현재 구현 상태와 미결정 사항을 확인한다.
- 특정 사용자의 절대 경로, 개인 메모리, 대화 이력, 인증 세션에 실행을 의존시키지 않는다.
- 코드와 공통 업무 지식은 공유하고, 인증정보 및 판매 데이터는 사용자별로 분리한다.
- 개인 설정과 실행 데이터는 local/ 또는 사용자 전용 외부 경로에 둔다. 인증정보를 코드, 예시, 로그에 기록하지 않는다.
- 공유 데이터 연동은 명시된 요구에 따라 설계한다. 코드 배포를 데이터 동기화로 설명하지 않는다.
- 새 기능에는 필요한 설정과 실행 방법을 함께 기록한다. 실제 검증한 범위를 넘어 완료를 주장하지 않는다.
